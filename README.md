# 哈基米大冒险

基于 Cocos Creator 3.8.8 开发的 2D 横版跑酷 Demo。项目目标是做成可交付演示版本，包含主菜单、商店/升级、设置、结算、跑酷玩法、金币收集、道具、障碍、导弹预警、皮肤和排行榜等内容。

## 打开项目

1. 安装 Cocos Creator 3.8.8。
2. 使用 Cocos Dashboard 打开本仓库根目录。
3. 等待资源导入完成。
4. 打开 `assets/scenes/Main.scene`。
5. 点击预览运行。

## Web 演示

如果已经构建出 `web-desktop-001`，云端预览配置位于 `.vscode/preview.yml`，会在该目录下启动静态服务：

```bash
python -m http.server 5000 --bind 0.0.0.0
```

本地 Web 构建产物也可以使用 `run_web_demo.bat` 辅助启动。

## 已提交内容

- `assets/`：场景、脚本、图片、字体资源以及 Cocos `.meta` 文件。
- `settings/v2/`：Cocos 项目设置。
- `package.json`：项目名称、UUID 和 Creator 版本。
- `.creator/default-meta.json`：默认资源 meta 设置。
- `.vscode/preview.yml`：云端 Web 预览配置。
- `start_web_demo.py` / `run_web_demo.bat`：Web 构建产物演示辅助脚本。

## 未提交内容

以下目录通常由 Cocos 或本机工具生成，clone 后会重新生成：

- `library/`
- `temp/`
- `local/`
- `build/`
- `native/`
- `profiles/`
- `extensions/cocos-mcp-server/`
- `node_modules/`

## 更新说明

### 2026-06-10

- 接入 14 张成就图标：将用户提供的绿幕底徽章抠成透明 PNG，裁掉多余空白并按成就顺序加入成就星光册；成就标题和说明整体右移，为左侧图标预留空间。
- 重构商店界面：按“购买道具 / 道具升级 / 角色选择 / 皮肤”分区展示，避免道具升级和皮肤混在一起。
- 新增购买道具：开局冲刺 200m、局内复活券、开局护盾，并接入本地库存存档与自动消耗逻辑。
- 扩展开局安全路段：开局先展示无障碍金币演出，加入 `1 2 3 GO` 和大型猫猫头金币像素阵列。
- 扩展金币像素画阵列：新增巨型猫猫头、爪印、鱼骨、蝴蝶结等大版面图案，并支持金 / 银 / 铜三色金币表现。
- 新增主菜单成就入口：设计多项成就条目，完成后授予星星并写入本地存档。
- 优化下滑动画逻辑：按下进入下滑过渡，长按保持稳定下滑帧，松开后播放起身过渡，减少保持下滑时闪烁。
- 更换 UI 字体：从装饰感较强的 `ZCOOL KuaiLe` 切换为更清晰稳重的 `DengXian Bold`，提升菜单、商店、设置和结算文本可读性。
- 调整商店交互：顶部改为“购买道具 / 道具升级 / 角色选择 / 皮肤”四个栏目，选中后只展示对应内容。
- 重做成就星光册：修复标题换行，改为可滚动的单列成就卡片，每个成就展示大字号名称、小字号说明和星星奖励。
- 重做开局金币演出：按整幅方格像素图生成金币底板，并用银币位格拼出 `1 2 3 GO` 字样。
- 调整道具平衡：降低局内道具生成概率，移除早期连续道具刷点，并缩短磁铁、护盾、双倍分、冲刺和开局消耗道具的持续时间。
- 接入新的主菜单、商店、设置、暂停、排行榜、成就、结算和 HUD 框体素材，替换旧的程序化面板，统一可爱风 UI 风格。
- 新增休闲模式入口：复用主场景与哈基米形象，加入普通云、移动云、易碎云和弹力云，形成轻量涂鸦跳跃玩法。
- 优化休闲模式重开逻辑和踩云体验：休闲模式结算后“再来一局”会回到休闲模式，云朵生成更可达，跳跃高度和踩云判定更宽容。
- 重构跳跃 / 二段跳 / 滑翔输入判定：空中短按触发二段跳，长按触发滑翔，没有二段跳储备时只响应长按滑翔。
- 替换本局结算界面素材：使用新的结算背景、结算大框和四条数据栏，数据填入预留区域，底部保留再跑一次、商店、排行、主菜单按钮。
- 优化即时成就通知：解锁成就时顶部弹出通知栏，使用对应成就图标；通知字号、图标和容器已放大，避免 `SHRINK` 自动缩小导致看不清。
- 替换复活券、成就星星、休闲云朵等白模素材，并接入新的 BGM，使主菜单和游戏内都播放统一背景音乐。
- 普通模式卡顿初步排查：高风险点集中在开局大规模金币字阵、实时 HUD/成就刷新、金币/障碍逐帧碰撞检测和特效浮字节点创建；已记录为后续专项优化项，本轮未继续扩大性能改造范围。

### 2026-06-04

- 同步远端更新：新增草莓、薄荷皮肤的完整动作资源与 Cocos `.meta`。
- 优化皮肤展示与切换逻辑，使不同皮肤使用独立动作序列而不是仅做颜色染色。
- 优化排行榜展示结构，提升结算页和排行榜信息的可读性。
- 扩展设置页：新增操作按键查看、背景音乐/音效音量调节、中英文切换、操作辅助提示开关和数据管理入口。
- 优化设置页操作按键说明：放大文字、扩大多行 Label 容器，避免 `SHRINK` 导致说明内容被自动缩小。

### 2026-05-28

- 接入可爱风格中文字体 `ZCOOL KuaiLe`，统一替换 HUD、按钮、商店、设置、结算和排行榜文字。
- 修复左上分数栏宽度不足导致的缩放闪烁问题，并在分数下方新增里程显示。
- 重绘并替换磁铁、护盾、冲刺、双倍分四类道具特效，增加角色跟随动效。
- 新增导弹危险机制：到达一定里程后出现闪烁预警线，随后导弹从右向左发射，护盾和冲刺可抵挡。
- 调整无地面背景与新跑道的对齐方式，避免背景底部被跑道遮挡。
- 更新主菜单标题图、无缝跑道素材和云端 Web 预览配置。
